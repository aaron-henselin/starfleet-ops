@model Starfleet.Ops.ViewComponents.HealthBarViewModel
    
    <div class="health-bar">
        @for (int i = 0; i < Model.Max; i++)
        {
            <div class="health-box @(Model.Current > i ? "" : "damaged")"></div>
        }
    </div>